<!DOCTYPE html>
<html class="no-js" lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="x-ua-compatible" content="ie=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= page_title.blank? ? nil : page_title + " – ".html_safe %>Scoutinv</title>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag "application", media: "all", "data-turbolinks-track": "reload" %>
  </head>
  <body>
    <div class="top-bar" id="responsive-menu">
      <div class="top-bar-left">
        <ul class="dropdown menu" data-dropdown-menu>
          <li class="menu-text">Scoutinv</li>
          <li><%= link_to "Home", root_path %></li>
          <% if user_signed_in? %>
            <li><%= link_to "Events", events_path %></li>
            <li><%= link_to "Products", products_path %></li>
            <li><%= link_to truncate(current_group.name, length: 30), current_group %></li>
          <% end %>
        </ul>
      </div>
      <div class="top-bar-right">
        <ul class="menu">
          <% if user_signed_in? && current_user.email == "francois@teksol.info" %>
            <li><%= link_to "Groups", groups_path %></li>
          <% end %>
        </ul>
      </div>
    </div>

    <div class="grid-container">
      <div class="grid-x grid-margin-x">
        <div class="large-12 cell">
          <h1><%= page_title || "Scoutinv" %></h1>
        </div>

        <% if alert.present? || notice.present? %>
          <div class="large-12 cell">
            <% if notice.present? %>
              <div class="callout success" data-closeable>
                <p><%= notice %></p>
              </div>
            <% end %>
            <% if alert.present? %>
              <div class="callout alert" data-closeable>
                <p><%= alert %></p>
              </div>
            <% end %>
          </div>
        <% end %>
      </div>
    </div>

    <div class="grid-container">
      <%= yield %>
    </div>

    <footer>
      <div class="grid-container">
        <div class="grid-x grid-margin-x">
          <div class="small-12 cell">
            <hr>
          </div>

          <div class="auto cell">
            <p>
            Scoutinv is a tool released under the GPLv2 open-source license, built by <%= mail_to "francois@teksol.info", "François Beausoleil" %>.
            Find Scoutinv on <%= link_to "GitHub", "https://gitlab.com/francoisb/scoutinv" %>.
            </p>
            <p>&copy; 2018<%= Date.today.year > 2018 ? " - #{Date.today.year}" : nil %> François Beausoleil. All rights reserved.</p>
          </div>
          <div class="shrink cell">
            <% if user_signed_in? %>
              <%= button_to "Logout", session_path(current_user.slug), class: "button secondary tiny", method: :delete %>
            <% end %>
          </div>
        </div>
      </div>
    </footer>

    <%= javascript_include_tag "application", "data-turbolinks-track": "reload" %>
  </body>
</html>
