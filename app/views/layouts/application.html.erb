<!DOCTYPE html>
<html class="no-js" lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="x-ua-compatible" content="ie=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= page_title.blank? ? nil : page_title + " – ".html_safe %>Scoutin</title>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag "application", media: "all", "data-turbolinks-track": "reload" %>
  </head>
  <body>
    <div class="title-bar" data-responsive-toggle="responsive-menu" data-hide-for="medium">
      <button class="menu-icon" type="button" data-toggle="responsive-menu"></button>
      <div class="title-bar-title">Menu</div>
    </div>

    <div class="top-bar" id="responsive-menu">
      <div class="top-bar-left">
        <ul class="dropdown menu" data-dropdown-menu>
          <li class="menu-text">Scoutin</li>
          <% if user_signed_in? %>
            <li><%= link_to "Events", events_path %></li>
            <li><%= link_to "Products", products_path %></li>
            <li><%= link_to truncate(current_group.name, length: 30), current_group %></li>
          <% end %>
        </ul>
      </div>
      <div class="top-bar-right">
        <ul class="menu">
          <% if user_signed_in? && current_user.email == "francois@teksol.info" %>
            <li><%= link_to "Groups", groups_path %></li>
          <% end %>
        </ul>
      </div>
    </div>

    <div class="grid-container">
      <div class="grid-x grid-padding-x">
        <div class="large-12 cell">
          <h1><%= page_title || "Scoutin" %></h1>
        </div>
      </div>

      <% if alert.present? || notice.present? %>
        <div class="grid-x grid-padding-x">
          <div class="large-12 cell">
            <% if notice.present? %>
              <div class="callout success" data-closeable>
                <p><%= notice %></p>
              </div>
            <% end %>
            <% if alert.present? %>
              <div class="callout alert" data-closeable>
                <p><%= alert %></p>
              </div>
            <% end %>
          </div>
        </div>
      <% end %>

      <%= yield %>

      <% if user_signed_in? %>
        <p><%= button_to "Logout", session_path(current_user.slug), class: "button secondary tiny", method: :delete %></p>
      <% end %>
    </div>

    <%= javascript_include_tag "application", "data-turbolinks-track": "reload" %>
  </body>
</html>
